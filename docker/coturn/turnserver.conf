listening-port=3478
listening-ip=0.0.0.0

realm=webrtc.local

# Long-term credentials — must match TURN_USERNAME and TURN_PASSWORD in .env
lt-cred-mech
user=webrtc:changeme

# Disable TCP relay; WebRTC media only needs UDP
no-tcp
no-tcp-relay

log-file=/dev/stdout
verbose
